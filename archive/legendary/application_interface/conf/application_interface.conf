# application_interface configuration

server tls {
	bind 127.0.0.1 8888
}

load		./application_interface.so

tls_dhparam	dh2048.pem

domain * {
	attach		tls

	certfile	cert/server.pem
	certkey		cert/key.pem

    # Routes will be generated by domain objects
	route	/Profile    view_restful
}
